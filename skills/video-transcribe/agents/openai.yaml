interface:
  display_name: "Video Transcribe"
  short_description: "Create SRT and TXT from media files"
  default_prompt: "Use $video-transcribe to convert this video or audio file into .srt and .txt transcripts."
policy:
  allow_implicit_invocation: true
